database:
  driver: "postgres"
  connection_string: "host=localhost port=5433 user=guts_spawner password=guts_spawner dbname=guts sslmode=disable"
virtualisation:
  memory: 4096
  cores: 8
general:
  image_cache_path: /srv/guts/images/
